# Database Configuration
# Copy this file to .env and update with your database credentials

# This project uses SQL Server only
# Database Connection Type
DB_CONNECTION=sqlsrv

# SQL Server Configuration
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourPassword123
DB_NAME=smart_room_booking
DB_ENCRYPT=false
DB_TRUST_CERT=true

# Session Secret
SESSION_SECRET=your-secret-key-here

# Node Environment
NODE_ENV=development

# ------------------------------------------------------------
# Real Control API (optional)
# ตั้งค่าเพื่อ forward คำสั่ง control ไฟ/แอร์/ERV ไป API จริง
# ถ้าไม่ตั้ง จะใช้ DB state ภายในระบบ (เหมาะสำหรับ dev/demo)
#
# Control endpoint (POST) - รับ payload ที่ backend ส่งให้
# - roomId, deviceType, deviceIndex, status, settings, context
CONTROL_API_CONTROL_URL=
#
# Status endpoint (GET) - ดึงสถานะอุปกรณ์จาก API จริง
# - backend จะส่ง query params: roomId, building, floor, area, room
CONTROL_API_STATUS_URL=
#
# Optional Bearer token
CONTROL_API_BEARER_TOKEN=
#
# Optional timeout (ms)
CONTROL_API_TIMEOUT_MS=8000

# ------------------------------------------------------------
# CCTV / การนับคน (Image Processing)
# ใช้ดึงภาพนิ่งจากกล้อง (JPEG) เพื่อวาดพื้นที่และนับคน
#
CCTV_BASE_URL=http://192.168.24.1
CCTV_USERNAME=admin
CCTV_PASSWORD=L@nnac0m

