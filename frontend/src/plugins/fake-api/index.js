// MSW/fake-api is disabled - we use the real backend API
// To enable mock API: restore msw + handlers from git history, run "npm run msw:init", then uncomment worker.start()
export default function () {}
